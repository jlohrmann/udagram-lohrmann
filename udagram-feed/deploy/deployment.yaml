apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    service: udagram-feed 
  name: udagram-feed 
spec:
  replicas: 1
  selector: 
    matchLabels:
      app: udagram-feed
  template:
    metadata:
      labels:
        app: udagram-feed 
        service: udagram-feed
    spec:
      containers:
      - image: hello198/udagram-feed
        name: udagram-feed
        imagePullPolicy: Always          
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "1024Mi"
            cpu: "500m"       
        ports:
        - containerPort: 8080
      restartPolicy: Always


